# Phase 6 — Implementation Roadmap

## Delivery order
1. Bootstrap Next.js + TypeScript + TailwindCSS.
2. Establish theme tokens and base layout (header, footer, container).
3. Implement `Home` page sections: Hero, Metrics, About, Featured Projects, Timeline preview, Contact CTA.
4. Implement `Projects` listing page with project cards.
5. Implement project detail pages for myU, MyU ID, Hung Hau House, Events, Face Verification (optional).
6. Implement `Timeline` and `Contact` pages.
7. Wire content from Phase 2/3 JSON/Markdown to UI.
8. QA: theme, responsive, motion, accessibility, performance.

## Suggested component slices
- Core shell: Navbar, Footer, SectionContainer, PageTransition
- Content components: Hero, AboutBlurb, MetricCard, ProjectCard, TimelineCard, ContactAction
- Project page components: ProjectHeader, SectionBlock, ImpactList, RelatedProjects
