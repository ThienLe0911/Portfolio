# Phase 2 — Metrics (Draft)

Goal: Select up to 4 metrics to surface on the landing page to quickly communicate impact.

Selected metrics

- **Active users (snapshot)**: 184,908 users — Monthly active users across the myU ecosystem (source: `Docs/myu & id-myu/tonghopDuanMyu.md`).
- **Survey responses (events)**: 270,524 responses — Total responses collected by Events Platform (source: `Docs/events-myu-vn/tonghopDuanTaoFormCustom.md`).
- **Featured projects**: 4 projects — myU, MyU ID, Events Platform, Hung Hau House (source: `Docs/guide/MasterPlan.md`).

Notes
- Metrics should be surfaced with short labels and an optional tooltip or microcopy that explains the time window/source.
- If you want to highlight a unit (e.g., revenue), prefer human-readable formatting on the site (e.g., `93.14B VND`).
- We can add dynamic updating later if you expose metrics from a data endpoint or CSV.
